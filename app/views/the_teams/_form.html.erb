<%= form_for(@the_team) do |f| %>
  <% if @the_team.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@the_team.errors.count, "error") %> prohibited this the_team from being saved:</h2>

      <ul>
      <% @the_team.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
