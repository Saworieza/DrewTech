<h1>Our Portfolio</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <!-- These idiots can be updated using an admin account, i mean admin account... keep them around until you create one. users cant even know about it.
  <tbody>
    <% @portfolios.each do |portfolio| %>
      <tr>
        <td><%= link_to 'Show', portfolio %></td>
        <td><%= link_to 'Edit', edit_portfolio_path(portfolio) %></td>
        <td><%= link_to 'Destroy', portfolio, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>-->
</table>

<br>

<!--as i said above, admin account <%= link_to 'New Portfolio', new_portfolio_path %>-->
